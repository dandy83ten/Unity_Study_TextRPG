namespace TextRPG.Models;

public enum JobType
{
    Warrior,
    Archer,
    Wizard,
}

// 아이템 타입
public enum ItemType
{
    Weaon,
    Armor,
    Potion
}

// 장비 슬롯 타입
public enum EquipmentSlot
{
    Weapon,
    Armor,
}